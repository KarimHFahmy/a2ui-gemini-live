# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Node
node_modules/
frontend/dist/

# Build output (produced by `make build`, baked into the image)
backend/static/

# Local configuration
.env
.env.local

# Tooling
.DS_Store
*.log

# Agent skills installed with `npx skills add`. Local tooling rather than
# product: they are design opinions this repo was audited against, not code it
# ships. Remove these lines to share a specific set with the team.
/.agents/
/.claude/skills/
/skills-lock.json
